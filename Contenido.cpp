#include "Contenido.h"
#include <iostream>

using namespace std;

Contenido :: Contenido (string titulo, int fecha, string id, float promedio) : titulo (titulo), fecha(fecha), id(id), promedio(promedio) {}

